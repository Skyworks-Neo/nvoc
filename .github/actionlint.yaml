self-hosted-runner:
  labels:
    - cuda
    - gpu-nvidia
    - opencl
    # GitHub-hosted slim runner; not yet in actionlint's built-in label list.
    - ubuntu-slim
    # GitHub-hosted Windows arm64 public preview runner.
    - windows-11-arm

config-variables: null
