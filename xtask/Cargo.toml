[package]
name = "xtask"
version = { workspace = true }
edition = { workspace = true }
rust-version = { workspace = true }
license = "Apache-2.0"
publish = false

description = "Local development orchestrator for the nvoc workspace (setup/build/check/test/run)"

[dependencies]
